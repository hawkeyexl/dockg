# package.json declares "engines": { "node": ">=24" }, but npm treats that as
# advisory by default and installs anyway. Make it binding so a too-old Node
# fails at install time rather than somewhere deep in a build.
engine-strict=true
